# Items parameters

itemid:
  name: itemid
  in: path
  description: Item ID
  required: true
  schema:
    type: string

folderid:
  name: folderid
  in: path
  description: Folder ID
  required: true
  schema:
    type: string

search:
  name: search
  in: query
  description: Search string
  required: false
  schema:
    type: string