# Users parameters

userid:
  name: userid
  in: path
  description: User ID
  required: true
  schema:
    type: string

search:
  name: search
  in: query
  description: Search string
  required: false
  schema:
    type: string