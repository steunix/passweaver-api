post:
  tags:
    - Utilities
  operationId: systemreadwrite
  summary: Set system in read write mode
  description: Set system in read write mode
  security:
    - bearerAuth: []
  responses:
    "200":
      $ref: '..\responsebodies\default.yaml#/success'
    "401":
      $ref: '..\responsebodies\default.yaml#/unauthorized'
      description: User cannot be logged in, wrong credentials
