# Success response
status:
  type: string
  enum: [ "success" ]

oneTimeSecretGetResult:
  description: Success
  content:
    application/json:
      schema:
        type: object
        properties:
          status:
            $ref: '#/status'
          data:
            type: string
            description: Secret
            example: The secret
