# Group parameters

groupid:
  name: groupid
  in: path
  description: Group ID
  required: true
  schema:
    type: string

parentgroupid:
  name: parentgroupid
  in: path
  description: Parent group ID
  required: true
  schema:
    type: string
